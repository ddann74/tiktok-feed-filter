<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed only to locate the video file TikTok's own Save action writes, so its
         audio can be extracted - this app never requests broad storage access for any
         other reason, and never touches the network. Split by API level since the
         permission model for reading other apps' media changed at 33 (READ_MEDIA_VIDEO
         replaced READ_EXTERNAL_STORAGE for this purpose) and again effectively at 29
         (scoped storage) - both are declared so whichever applies on the device is
         available; the OS ignores whichever one doesn't apply to its version. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- No other permissions are needed: the accessibility service reads on-screen
         text via the accessibility APIs (granted separately, in Settings) and
         nothing here ever touches the network. -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".TikTokFilterService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Lets "Share Diagnostic Log" hand the file to another app (email, Files,
             a text editor) via a content:// Uri, without making the file world-readable -
             exported="false" plus per-request grantUriPermissions is the standard,
             narrowly-scoped way to share a single app-private file. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
